import { makeStyles } from "@mui/styles";

const homePageStyle = makeStyles({
  surface: {
    marginTop: "60px",
    display: "flex",
    width: "80vw",
    marginLeft: "10vw",
  },
});

export default homePageStyle;
