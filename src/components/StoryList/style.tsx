import { makeStyles } from "@mui/styles";

const useStoryListStyles = makeStyles({
  defaultCard: {
    backgroundColor: "#e5e5e5",
    padding: "10px",
    borderRadius: "10px",
    display: "flex",
    margin: "10px",
    justifyContent: "center",
    marginTop: "20px",
  },
});

export default useStoryListStyles;
